# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.python-version

# Spark / data outputs
spark-warehouse/
metastore_db/
derby.log
*.parquet
*.crc
_SUCCESS

# Données générées par le pipeline (exports + checkpoints Spark) — voir docs/contrat-export.md.
# Le dossier est suivi (via .gitkeep) mais son contenu ne l'est pas : ce sont des artefacts
# d'exécution, régénérés à chaque `docker compose up`.
data/*
!data/.gitkeep

# Docker
*.pid

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Livrables zip volumineux (gardés hors du dépôt git, déposés sur OC)
livrables/*.zip
